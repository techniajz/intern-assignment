<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Real Estate Listing</title>
  <link rel="stylesheet" href="styles.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #efc9c3;
      display: flex;
      justify-content: center;
      align-items: center;
     
    }

    .listing-container {
      width: 100%;
      padding: 20px;
      border-radius: 8px;
      position: relative;
    }

    .back-button {
      position: absolute;
      top: 60px;
      left: 40px;
      padding: 8px 12px;
      background-color: #333;
      color: #fff;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 24px;
    }

    h1 {
      text-align: center;
      color: #333;
      font-size: 55px;
    }

    ul {
      list-style: none;
      padding: 10px;
    }

    li {
      margin-bottom: 20px;
      display: flex;
      justify-content: space-between;
      padding: 10px;
    }

    strong {
      font-size: 30px;
      text-align: left;
      width: 50%;
    }

    @media (max-width: 768px) {
      .listing-container {
        width: 90%;
      }
    }
  </style>
</head>

<body>

  <div class="listing-container">
    <button class="back-button" onclick="goBack()">Back</button>
    <h1>Lead Info</h1>
    <ul>
      <li>
        <strong>Name:</strong>
        <strong><%=shareProject?.name%></strong>
      </li>
      <li>
        <strong>Location URL:</strong>
        <strong><a href="<%=shareProject.location_URL%>" target="_blank">View Location</a></strong>
      </li>
      <li>
        <strong>Price Range:</strong>
        <strong><%=shareProject?.minPrice%> - <%=shareProject?.maxPrice%></strong>
      </li>
      <li>
        <strong>Property Size:</strong>
        <strong>
        <!-- <strong><%=shareProject?.size%></strong> -->
          <% if (shareProject.size && shareProject.size.length > 0) { %>
            <ul>
              <% for (let i = 0; i < shareProject.size.length; i++) { %>
                  <strong><%= shareProject.size[i]?.type %></strong>,
              <% } %>
            </ul>
          <% } else { %>
            <p>No properties found.</p>
          <% } %>
        </strong>
      </li>
      <li>
        <strong>Year Built:</strong>
        <strong><%=shareProject?.yearBuilt%></strong>
      </li>
      <li>
        <strong>Owner of Property:</strong>
        <strong><%=shareProject?.OwnerOfProperty%></strong>
      </li>
      <li>
        <strong>Nearby:</strong>
        <strong><%=shareProject?.nearby%></strong>
      </li>
      <li>
        <strong>Certifications:</strong>
        <strong><%=shareProject?.certifications%></strong>
      </li>
      <li>
        <strong>Contact Person:</strong>
        <strong><%=shareProject?.contactPerson%></strong>
      </li>
      <li>
        <strong>Contact Number:</strong>
        <strong><%=shareProject?.contactNumber%></strong>
      </li>
      <li>
        <strong>Description:</strong>
        <strong><%=shareProject?.description%></strong>
      </li>
      <li>
        <strong>Amenities:</strong>
        <strong><%=shareProject?.amenities%></strong>
      </li>
      <li>
        <strong>Notes:</strong>
        <strong><%=shareProject?.notes%></strong>
      </li>
      <li>
        <strong>Lead Name:</strong>
        <strong><%= shareProject && shareProject.leadId && shareProject.leadId.name ? shareProject.leadId.name : "" %></strong>
      </li>
      <li>
        <strong>Project Name:</strong>
        <strong><%=shareProject && shareProject.projectId && shareProject.projectId.name ? shareProject.projectId.name : ""%></strong>
      </li>
      <li>
        <strong>Lead Name:</strong>
        <strong>Sanjay</strong>
      </li>

    </ul>
  </div>

  <script>
    function goBack() {
      window.history.back();
    }
  </script>
</body>
</html>